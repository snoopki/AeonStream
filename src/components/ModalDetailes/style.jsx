import { CardContent, Dialog, IconButton, Typography } from "@mui/material";
import { styled } from "@mui/system";

export const EIconButton = styled(IconButton)`
  position: absolute;
  top: 1%;
  right: 3%;
`;

export const ECardContent = styled(CardContent)`
  position: absolute;
  top: 2%;
  right: 35;
`;

export const EDialog = styled(Dialog)`
  background-color: rgba(0, 0, 0, 0.9);
`;
