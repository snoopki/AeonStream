import { Button, DialogTitle, IconButton } from "@mui/material";
import { styled } from "@mui/system";

export const EButton = styled(Button)`
  margin-left: 20px;
  height: 52px;
  margin-top: 15px;
  font-weight: bold;
`;

export const EIconButton = styled(IconButton)`
  position: absolute;
  top: 10px;
  right: 30px;
`;
